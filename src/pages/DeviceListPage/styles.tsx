import styled from '@emotion/styled'
import {Container, Box } from '@mui/material';

export const DeviceListPageContainer = styled(Container)` 
    max-width: 1600px;
`
export const SearchWrapper = styled(Box)`
  display: flex;
  align-items: center;
  padding: 20px 0;
  gap: 40px;
`

export const TableManageWraper= styled(Box)`
  display: flex;
  align-items: center;
  margin-top: 60px;
`
